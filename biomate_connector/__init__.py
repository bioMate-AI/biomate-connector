"""biomate_connector — top-level package shim.

Provides the canonical import surface for the BioMate connector:
    from biomate_connector.oauth import OAuthServer, OAuthStore, Client
    from biomate_connector.oauth.router import router
"""
