# Auto-HTTPS for the Feishu connector. CONNECTOR_DOMAIN comes from .env.
# Caddy fetches + renews a Let's Encrypt cert automatically (needs ports 80+443
# reachable and the DNS A record pointing here).
{$CONNECTOR_DOMAIN} {
    encode gzip
    reverse_proxy feishu-bot:8093
}
