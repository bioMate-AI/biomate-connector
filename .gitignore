# Local environment / secrets — never commit
.env
.env.local
.env.*.local
# Local MCP client config — contains a live BIOMATE_API_KEY, dev-machine only
.mcp.json

# Python
__pycache__/
*.pyc
*.pyo
.venv/
*.egg-info/
dist/
build/
.pytest_cache/
